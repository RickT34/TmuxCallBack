# 长任务完成后自动唤醒

当你需要运行长时间命令，并在它退出后继续工作时，使用以下脚本后台执行。启动成功后可以结束当前轮次，无需让用户手动发消息唤醒你。

```bash
/path/to/TmuxCallback/tmux_callback \
  -c 'python train.py > train.log 2>&1' \
  -m '训练命令已退出，请检查 train.log 和产物，判断成功或失败并继续后续工作。'
```

- 将脚本路径替换为实际仓库路径。默认后台运行，日志固定追加到脚本所在目录的 `tmux-callback.log`，通常只需提供 `-c` 和 `-m`，无需指定后台模式或日志路径。
- 将 `-c` 替换为实际命令，`-m` 替换为简短的单行唤醒消息，写清唤醒后的动作。命令以启动目录为工作目录，通过 `/bin/sh` 执行；需要 Bash 时显式使用 `bash script.sh`。
- 默认从 `TMUX_PANE` 和 `TMUX` 定位你所在的 pane 和服务器。若未继承这些环境变量，必须显式传入正确的 `-t session:window.pane`，自定义服务器另加 `--socket /path/to/socket`。不要猜测目标。
- 命令末尾不要加 `&`，也不要重复启动同一任务。成功启动会返回后台 PID、目标 pane 和日志路径；这只表示已启动，不表示任务完成。
- 命令无论成功或失败退出，脚本都会先发送字面文本。
